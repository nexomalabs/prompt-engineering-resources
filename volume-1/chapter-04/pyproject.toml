[project]
name = "aiae-vol1-lab04"
version = "1.0.0"
description = "Applied AI Engineering — Volume I, Lab 4: Building a Neural Network"
requires-python = ">=3.12"
dependencies = ["numpy>=2.0"]

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
