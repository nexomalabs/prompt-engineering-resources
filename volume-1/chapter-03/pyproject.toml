[project]
name = "aiae-vol1-lab03"
version = "1.0.0"
description = "Applied AI Engineering — Volume I, Lab 3: Training Your First Machine Learning Model"
requires-python = ">=3.12"
dependencies = ["scikit-learn>=1.5"]

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
